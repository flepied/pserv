#!/bin/sh

pkg="$1"

. pkg/$pkg/config

dest="adminkit-$pkg-$version"

rm -rf build/$dest
mkdir build/$dest
tar c -C pkg/$pkg .|tar x -C build/$dest
cp -r skel/* build/$dest/
rm build/$dest/debian/*.ex build/$dest/debian/*.EX

perl -p -i -e "s/\@pkg\@/$pkg/g;s/\@version\@/$version/g" build/$dest/debian/control build/$dest/debian/changelog
cd build/$dest
dpkg-buildpackage -rfakeroot

# create-pkg ends here
